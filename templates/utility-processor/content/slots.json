{
  "template": "utility-processor",
  "version": "1.0.0",
  "totalSlots": 64,
  "slots": {
    "brand": {
      "app_name": {
        "type": "headline",
        "maxLength": 30,
        "default": "Utility Processor",
        "description": "Application name displayed in header and title (dropzone-first)"
      },
      "tagline": {
        "type": "body",
        "maxLength": 60,
        "default": "Upload → process → download in one screen",
        "description": "Primary tagline for marketing"
      },
      "description": {
        "type": "body",
        "maxLength": 100,
        "default": "Minimalist utility SaaS template with hero dropzone, determinate progress, and usage meter.",
        "description": "One-line product description"
      }
    },
    "landing": {
      "hero_title": {
        "type": "headline",
        "maxLength": 80,
        "default": "One-screen utility with a hero dropzone",
        "description": "Main hero headline"
      },
      "hero_subtitle": {
        "type": "body",
        "maxLength": 150,
        "default": "Auto-start for quick jobs, manual start when you need control. Determinate progress, retries, and quotas included.",
        "description": "Hero supporting text"
      },
      "cta_primary": {
        "type": "button",
        "maxLength": 25,
        "default": "Try a sample conversion",
        "description": "Primary CTA button text"
      },
      "cta_secondary": {
        "type": "button",
        "maxLength": 25,
        "default": "View patterns",
        "description": "Secondary CTA button text"
      },
      "feature_1_title": {
        "type": "feature_title",
        "maxLength": 40,
        "default": "Hero dropzone",
        "description": "Feature 1 title"
      },
      "feature_1_description": {
        "type": "body",
        "maxLength": 150,
        "default": "Drag-over states, clipboard paste, and size/type validation before upload so users never hit surprise errors.",
        "description": "Feature 1 description"
      },
      "feature_2_title": {
        "type": "feature_title",
        "maxLength": 40,
        "default": "Waiting UX",
        "description": "Feature 2 title"
      },
      "feature_2_description": {
        "type": "body",
        "maxLength": 150,
        "default": "Deterministic progress for upload vs processing, smoothed progress bars, and soft-failure retries.",
        "description": "Feature 2 description"
      },
      "feature_3_title": {
        "type": "feature_title",
        "maxLength": 40,
        "default": "Quota clarity",
        "description": "Feature 3 title"
      },
      "feature_3_description": {
        "type": "body",
        "maxLength": 150,
        "default": "Usage meters, permeable paywall for early value, and gentle upsells instead of hard stops.",
        "description": "Feature 3 description"
      },
      "social_proof_title": {
        "type": "section_title",
        "maxLength": 50,
        "default": "Built from minimalist SaaS research",
        "description": "Social proof section title"
      }
    },
    "auth": {
      "login_title": {
        "type": "headline",
        "maxLength": 40,
        "default": "Welcome back",
        "description": "Login page title"
      },
      "login_subtitle": {
        "type": "body",
        "maxLength": 80,
        "default": "Sign in to your workspace",
        "description": "Login page subtitle"
      },
      "signup_title": {
        "type": "headline",
        "maxLength": 40,
        "default": "Create your account",
        "description": "Signup page title"
      },
      "signup_subtitle": {
        "type": "body",
        "maxLength": 80,
        "default": "Start organizing your work",
        "description": "Signup page subtitle"
      },
      "google_button": {
        "type": "button",
        "maxLength": 30,
        "default": "Continue with Google",
        "description": "Google OAuth button text"
      },
      "magic_link_button": {
        "type": "button",
        "maxLength": 30,
        "default": "Send magic link",
        "description": "Magic link button text"
      }
    },
    "dashboard": {
      "inbox_empty_title": {
        "type": "empty_state",
        "maxLength": 40,
        "default": "No jobs yet",
        "description": "Empty state title"
      },
      "inbox_empty_description": {
        "type": "body",
        "maxLength": 100,
        "default": "Drop a file into the hero dropzone to auto-start or switch to manual mode to stage jobs.",
        "description": "Empty state description"
      },
      "projects_empty_title": {
        "type": "empty_state",
        "maxLength": 40,
        "default": "No history yet",
        "description": "Empty history title"
      },
      "projects_empty_description": {
        "type": "body",
        "maxLength": 100,
        "default": "Completed and failed jobs appear here with retry so users never reselect files.",
        "description": "Empty history description"
      },
      "all_done_title": {
        "type": "empty_state",
        "maxLength": 40,
        "default": "All jobs processed",
        "description": "All jobs complete title"
      },
      "all_done_description": {
        "type": "body",
        "maxLength": 100,
        "default": "No pending jobs. Upload another file or adjust configuration for the next run.",
        "description": "All jobs complete description"
      }
    },
    "billing": {
      "free_plan_name": {
        "type": "headline",
        "maxLength": 20,
        "default": "Free",
        "description": "Free tier name"
      },
      "free_plan_description": {
        "type": "body",
        "maxLength": 60,
        "default": "First 3 jobs free with 500MB cap and auto-start uploads.",
        "description": "Free tier description"
      },
      "pro_plan_name": {
        "type": "headline",
        "maxLength": 20,
        "default": "Pro",
        "description": "Pro tier name"
      },
      "pro_plan_description": {
        "type": "body",
        "maxLength": 60,
        "default": "Credits, manual start, retries, and webhook receipts.",
        "description": "Pro tier description"
      },
      "business_plan_name": {
        "type": "headline",
        "maxLength": 20,
        "default": "Business",
        "description": "Business tier name"
      },
      "business_plan_description": {
        "type": "body",
        "maxLength": 60,
        "default": "Unlimited jobs, priority processing, SSO, and audit logs.",
        "description": "Business tier description"
      },
      "seat_limit_warning": {
        "type": "error",
        "maxLength": 100,
        "default": "File too large for this plan. Upgrade for higher caps and priority processing.",
        "description": "Limit warning tuned for file processing"
      },
      "upgrade_cta": {
        "type": "button",
        "maxLength": 25,
        "default": "Upgrade plan",
        "description": "Upgrade button text"
      }
    },
    "settings": {
      "general_title": {
        "type": "headline",
        "maxLength": 40,
        "default": "General Settings",
        "description": "General settings page title"
      },
      "team_title": {
        "type": "headline",
        "maxLength": 40,
        "default": "Team Members",
        "description": "Team settings page title"
      },
      "billing_title": {
        "type": "headline",
        "maxLength": 40,
        "default": "Billing",
        "description": "Billing settings page title"
      },
      "invite_button": {
        "type": "button",
        "maxLength": 25,
        "default": "Invite member",
        "description": "Invite team member button text"
      },
      "save_button": {
        "type": "button",
        "maxLength": 25,
        "default": "Save changes",
        "description": "Save button text"
      }
    },
    "errors": {
      "auth_error": {
        "type": "error",
        "maxLength": 100,
        "default": "Something went wrong. Please try again.",
        "description": "Generic auth error message"
      },
      "not_found_title": {
        "type": "error",
        "maxLength": 40,
        "default": "Page not found",
        "description": "404 page title"
      },
      "not_found_description": {
        "type": "error",
        "maxLength": 100,
        "default": "The page you're looking for doesn't exist.",
        "description": "404 page description"
      },
      "server_error_title": {
        "type": "error",
        "maxLength": 40,
        "default": "Something went wrong",
        "description": "500 page title"
      },
      "server_error_description": {
        "type": "error",
        "maxLength": 100,
        "default": "We're working on it. Please try again later.",
        "description": "500 page description"
      }
    },
    "keyboard": {
      "hint_create": {
        "type": "body",
        "maxLength": 25,
        "default": "Create issue",
        "description": "Create issue shortcut hint"
      },
      "hint_search": {
        "type": "body",
        "maxLength": 25,
        "default": "Search",
        "description": "Search shortcut hint"
      },
      "hint_navigate": {
        "type": "body",
        "maxLength": 25,
        "default": "Navigate",
        "description": "Navigation shortcut hint"
      },
      "hint_open": {
        "type": "body",
        "maxLength": 25,
        "default": "Open",
        "description": "Open item shortcut hint"
      },
      "hint_edit": {
        "type": "body",
        "maxLength": 25,
        "default": "Edit",
        "description": "Edit shortcut hint"
      }
    },
    "meta": {
      "title_template": {
        "type": "body",
        "maxLength": 50,
        "default": "%s | Utility Processor",
        "description": "Page title template"
      },
      "default_title": {
        "type": "headline",
        "maxLength": 60,
        "default": "Utility Processor - Upload → Process → Download",
        "description": "Default page title"
      },
      "meta_description": {
        "type": "body",
        "maxLength": 160,
        "default": "Minimalist utility SaaS template with hero dropzone, determinate progress, quota meters, and soft-failure retries.",
        "description": "Meta description for SEO"
      }
    },
    "inbox": {
      "title": {
        "type": "section_title",
        "maxLength": 20,
        "default": "Notifications",
        "description": "Notifications heading"
      },
      "subtitle": {
        "type": "body",
        "maxLength": 80,
        "default": "Stay on top of mentions and updates",
        "description": "Inbox page description"
      },
      "tabs.all": {
        "type": "button",
        "maxLength": 15,
        "default": "All",
        "description": "All notifications tab"
      },
      "tabs.mentions": {
        "type": "button",
        "maxLength": 15,
        "default": "Mentions",
        "description": "Mentions filter tab"
      },
      "tabs.assigned": {
        "type": "button",
        "maxLength": 15,
        "default": "Assigned",
        "description": "Assigned to me filter tab"
      },
      "tabs.watching": {
        "type": "button",
        "maxLength": 15,
        "default": "Watching",
        "description": "Subscribed items filter tab"
      },
      "empty.title": {
        "type": "section_title",
        "maxLength": 40,
        "default": "All caught up",
        "description": "Empty inbox heading"
      },
      "empty.description": {
        "type": "body",
        "maxLength": 100,
        "default": "No new notifications. Take a break or jump back into your work.",
        "description": "Empty inbox description"
      },
      "markRead.button": {
        "type": "button",
        "maxLength": 20,
        "default": "Mark all read",
        "description": "Mark all as read button"
      },
      "priority.urgent": {
        "type": "body",
        "maxLength": 15,
        "default": "Urgent",
        "description": "Urgent priority label"
      },
      "priority.high": {
        "type": "body",
        "maxLength": 15,
        "default": "High",
        "description": "High priority label"
      },
      "priority.medium": {
        "type": "body",
        "maxLength": 15,
        "default": "Medium",
        "description": "Medium priority label"
      },
      "priority.low": {
        "type": "body",
        "maxLength": 15,
        "default": "Low",
        "description": "Low priority label"
      }
    }
  }
}
